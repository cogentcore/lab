name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25.6'

    - name: Set up Core
      run: go install cogentcore.org/core@main && core setup

    - name: Build
      if: false
      run: go build -v ./...

    - name: Test
      run: go test -v ./... -coverprofile cover.out -timeout 30s

  coverage:
      uses: cogentcore/go-coverage-report/.github/workflows/action.yml@a585547e6b766b6472fcb1dc91c38f918099e0d9
      with:
        coverage-file: cover.out
      if: github.event_name == 'push'
