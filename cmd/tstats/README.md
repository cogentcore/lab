# tstats

```
tstats computes standard descriptive statistics on a column of data in a CSV / TSV file.

Usage:
    tstats <column> <files>

Flags:
    -column string
        Name of the column to compute stats on.
    -files []string
        Files to compute stats on.
```

